{
  "name": "lincoln-barista",
  "version": "1.9.4",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "for file in public/js/*.js public/sw.js; do node --check \"$file\" || exit 1; done",
    "test": "node --test"
  }
}
